import React from "react"

const IndexPage = () => (
  <main id="main-content" role="main">
        <h1 className="sr-only">Travel guidebook</h1>
        <p>Once deployed, this page is invisible.  The web server will re-direct to /en, where language detection is done.</p>
  </main>
)
export default IndexPage
